---
phase: 06-graph-visualization-upgrade
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - app/tool/page.tsx
autonomous: false
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "/tool renders the new <SemanticGraph> component instead of the inline SVG block"
    - "Pan, zoom, and drag are sigma-native — none of the SVG-only state (pan, zoom, isPanning, draggedNodeId, panStartRef, svgRef) remains in app/tool/page.tsx"
    - "The custom force simulation (simulateGraph) and its requestAnimationFrame loop are deleted; sigma's FA2 worker replaces them"
    - "Selecting a node still updates selectedNodeId and the evidence panel; clicking empty space deselects (D-14 preserved)"
    - "Show only gaps toggle still filters the graph — parent passes pre-filtered nodes/links to <SemanticGraph> per D-15"
    - "Export PNG button is visibly disabled (D-17 deferred to Phase 7) but its DOM slot is preserved to avoid layout shift"
    - "Existing evidence panel, model strength dash, and discourse panels render unchanged because they read from nodeMap/links/selectedLink/selectedNode (untouched)"
    - "200+ node graphs render at interactive frame rates (manual smoke test confirms VIS-01)"
    - "Louvain rings appear on graphs with >=3 communities and are absent on simpler ones (manual smoke test confirms VIS-03)"
  artifacts:
    - path: app/tool/page.tsx
      provides: "Tool page wired to <SemanticGraph>"
      contains: "<SemanticGraph"
  key_links:
    - from: app/tool/page.tsx (render block)
      to: components/tool/semantic-graph.tsx
      via: "named import + JSX render"
      pattern: "import \\{ SemanticGraph \\} from \"@/components/tool/semantic-graph\""
    - from: app/tool/page.tsx (visibleNodes/visibleLinks memos)
      to: <SemanticGraph nodes={visibleNodes} links={visibleLinks} />
      via: "props pass-through (D-15: parent filters)"
      pattern: "nodes=\\{visibleNodes\\}"
    - from: app/tool/page.tsx (selectNode)
      to: <SemanticGraph onNodeSelect={selectNode} />
      via: "callback wiring"
      pattern: "onNodeSelect=\\{selectNode\\}"
---

<objective>
Wire the new `<SemanticGraph>` component into `app/tool/page.tsx`, replacing the inline SVG block (lines ~1269-1410), and excise the now-dead SVG-only state and helpers (`pan`, `zoom`, `isPanning`, `draggedNodeId`, `panStartRef`, `svgRef`, `toGraphCoords`, the `simulateGraph` rAF loop, and the standalone `nodeColor` / `simulateGraph` functions). Preserve everything outside the SVG block: `selectedNodeId`, `selectedLinkId`, `selectNode`, `nodes`, `links`, `nodeMap`, `visibleNodes`, `visibleLinks`, the showGapsOnly toggle, the evidence panel, ModelStrengthDash, and all surrounding copy.

Purpose: Final integration delivering Phase 6's user-visible win (VIS-01/02/03). After this plan lands, the user sees a WebGL graph with entity-typed colors, community rings on medium graphs, and smooth pan/zoom on 200+ nodes.
Output: A modified `app/tool/page.tsx` and a manual checkpoint that confirms graph rendering, selection wiring, gaps-only filter, and Louvain ring behavior in a real browser session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-graph-visualization-upgrade/06-CONTEXT.md
@.planning/phases/06-graph-visualization-upgrade/06-RESEARCH.md
@.planning/phases/06-graph-visualization-upgrade/06-UI-SPEC.md
@.planning/phases/06-graph-visualization-upgrade/06-VALIDATION.md
@app/tool/page.tsx
@components/tool/semantic-graph.tsx
@lib/pipeline/models.ts

<interfaces>
SemanticGraph public API (provided by Plan 02):

```typescript
import { SemanticGraph } from "@/components/tool/semantic-graph";

<SemanticGraph
  nodes={visibleNodes}            // GraphNode[] — already filtered by showGapsOnly
  links={visibleLinks}            // GraphLink[] — already filtered
  selectedNodeId={selectedNodeId} // string | null
  showGapsOnly={showGapsOnly}     // accepted but unused by component (D-15)
  onNodeSelect={selectNode}       // (id: string) => void
/>
```

Existing app/tool/page.tsx anchors (verified line numbers from current state):
- Line 19-22: VIEW_WIDTH/VIEW_HEIGHT/CENTER_X/CENTER_Y constants — only used by the SVG/simulateGraph; safe to delete
- Line 609-616: `function nodeColor(category)` — replaced by sigma's entity-type fill; delete
- Line 617-700: `function simulateGraph(...)` — replaced by FA2 worker; delete
- Line 758: `selectedLinkId` state — KEEP (evidence panel uses it)
- Line 759: `nodes` state — KEEP
- Line 774-779: `zoom`, `pan`, `isPanning`, `draggedNodeId`, `svgRef`, `panStartRef` — DELETE (sigma-native)
- Line 800-810: requestAnimationFrame loop driving `simulateGraph` — DELETE
- Line 812: `nodeMap` memo — KEEP (used by evidence panel and link rendering)
- Line 825-826: `setPan({...})` and `setZoom(1)` calls in reset effect — DELETE those two lines
- Line 830-???: `exportGraphPng` — KEEP the function reference but make the button visibly disabled (D-17)
- Line 966-983: `visibleLinks` / `visibleNodes` memos — KEEP unchanged
- Line 1067-1079: `toGraphCoords` helper — DELETE (only SVG used it)
- Line 1081-???: `selectNode(nodeId)` — KEEP, exact callback passed to onNodeSelect
- Line 1259-1411: SVG container `<div>` wrapper + `<svg>` block — REPLACE with `<SemanticGraph ...>` inside the same wrapper div; keep the Export PNG button row above
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SVG block with SemanticGraph and excise SVG-only state</name>
  <files>app/tool/page.tsx</files>
  <read_first>
    - app/tool/page.tsx FULL (1627 lines — must understand what stays and what goes; do not remove anything outside the explicit DELETE list below)
    - components/tool/semantic-graph.tsx (Plan 02 public shell — confirms named export `SemanticGraph` and prop names)
    - .planning/phases/06-graph-visualization-upgrade/06-CONTEXT.md (D-13 sigma-native pan/zoom; D-14 selection callback; D-15 parent filtering; D-17 export PNG deferred)
  </read_first>
  <action>
    All edits target `app/tool/page.tsx`. Apply in this order:

    ### Step 1 — Add the import (top of file, with other component imports)

    Add: `import { SemanticGraph } from "@/components/tool/semantic-graph";`

    ### Step 2 — DELETE these constants at lines ~19-22

    ```ts
    const VIEW_WIDTH = 1180;
    const VIEW_HEIGHT = 760;
    const CENTER_X = VIEW_WIDTH / 2;
    const CENTER_Y = VIEW_HEIGHT / 2;
    ```

    Search for any other reference to `VIEW_WIDTH`, `VIEW_HEIGHT`, `CENTER_X`, `CENTER_Y` and remove them. The file `lib/pipeline/structurer.ts` line 1265 mentions a 1180x760 canvas in the prompt — DO NOT modify that string; it is the LLM coordinate hint and stays as-is.

    Note: `simulateGraph` at lines ~684-685 references `VIEW_WIDTH`/`VIEW_HEIGHT` but you are deleting `simulateGraph` entirely in Step 4, so those references vanish with the function.

    ### Step 3 — DELETE the standalone `nodeColor` function (lines ~609-616)

    The full block:
    ```ts
    function nodeColor(category: NodeCategory): string {
      // ...
    }
    ```
    Sigma's entityFillColor (in semantic-graph-inner.tsx) replaces it.

    ### Step 4 — DELETE the standalone `simulateGraph` function (lines ~617-700)

    The full multi-line `function simulateGraph(...)` definition.

    ### Step 5 — DELETE the SVG-only state declarations (lines ~774-779)

    ```ts
    const [zoom, setZoom] = useState(1);
    const [pan, setPan] = useState({ x: 0, y: 0 });
    const [isPanning, setIsPanning] = useState(false);
    const [draggedNodeId, setDraggedNodeId] = useState<string | null>(null);
    const svgRef = useRef<SVGSVGElement | null>(null);
    const panStartRef = useRef({ x: 0, y: 0 });
    ```

    Also remove any unused imports (`useRef` if no other refs remain — check with grep; `useRef` is also used elsewhere so likely stays).

    ### Step 6 — DELETE the requestAnimationFrame simulation loop

    Locate the useEffect at lines ~798-810 that schedules the rAF animate function and calls `simulateGraph(...)`. Delete the entire useEffect block.

    ### Step 7 — DELETE pan/zoom resets

    In the reset effect at lines ~823-826, delete the two lines:
    ```ts
    setPan({ x: 0, y: 0 });
    setZoom(1);
    ```
    Keep the rest of the reset logic (setSelectedLinkId(null), setSelectedNodeId("brand-core"), etc.).

    ### Step 8 — DELETE the `toGraphCoords` helper (lines ~1067-1079)

    The full inner-component arrow function. Sigma handles coordinates natively.

    ### Step 9 — REPLACE the SVG block

    Locate the wrapper at line 1259:
    ```jsx
    <div className="overflow-hidden rounded-xl border border-border bg-surface-inset">
    ```

    Inside this wrapper, KEEP the toolbar `<div className="flex justify-end px-3 pt-2">` containing the Export PNG button, but modify the button to be disabled (D-17). Replace the existing `<button>` with EXACTLY:

    ```jsx
    <button
      type="button"
      disabled
      title="PNG export returns in Phase 7"
      className="rounded-lg border border-border bg-card px-3 py-1.5 text-xs font-medium text-foreground opacity-50 cursor-not-allowed"
    >
      Export PNG
    </button>
    ```

    Then DELETE the entire `<svg ref={svgRef} ...> ... </svg>` block (lines ~1269-1410, from `<svg` opening tag through its matching `</svg>`) and replace it with:

    ```jsx
    <SemanticGraph
      nodes={visibleNodes}
      links={visibleLinks}
      selectedNodeId={selectedNodeId || null}
      showGapsOnly={showGapsOnly}
      onNodeSelect={selectNode}
    />
    ```

    The wrapper `<div>` and the toolbar div remain. The closing `</div>` of the wrapper at line 1411 stays.

    ### Step 10 — DELETE the now-unused `exportGraphPng` function

    Search for `const exportGraphPng = ...` at line ~830. The function targeted the SVG element via `svgRef`. Since the button is now disabled and svgRef is gone, delete the entire function. The Export PNG button no longer references it.

    ### Step 11 — DELETE the `selectedLinkId` link click handler reference if it lived only in the SVG

    Verify: `selectedLinkId` is read by the evidence panel (line ~1430-1440). KEEP `selectedLinkId` state, `setSelectedLinkId` calls in `selectNode` and reset effect, and the `selectedLink` memo. The link-selection click was inside the SVG block, so it was deleted in Step 9. Edge selection from clicks is dropped in Phase 6 (sigma's clickEdge wiring is a future enhancement); this is acceptable because only node selection is in Phase 6 success criteria. Add a brief code comment above the surviving `selectedLinkId` state: `// selectedLinkId is preserved for the evidence panel; edge-click wiring returns in a future phase.`

    ### Step 12 — Final cleanup

    - Remove any `import { ... }` lines that now reference deleted symbols (`NodeCategory` may still be used elsewhere; verify before removal — `grep "NodeCategory" app/tool/page.tsx`).
    - Run `npx tsc --noEmit` and fix any "unused variable" or unresolved-reference errors that arise from the deletions. Each fix should be either a removed import or an added ESLint pragma — do NOT re-introduce deleted state.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; npm run test:contract &amp;&amp; grep -c "SemanticGraph" app/tool/page.tsx &amp;&amp; ! grep -q "function simulateGraph" app/tool/page.tsx &amp;&amp; ! grep -q "function nodeColor" app/tool/page.tsx &amp;&amp; ! grep -q "svgRef" app/tool/page.tsx &amp;&amp; ! grep -q "panStartRef" app/tool/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { SemanticGraph } from "@/components/tool/semantic-graph";' app/tool/page.tsx` returns `1`
    - `grep -c "<SemanticGraph" app/tool/page.tsx` returns `1`
    - `grep -c "nodes={visibleNodes}" app/tool/page.tsx` returns `1`
    - `grep -c "links={visibleLinks}" app/tool/page.tsx` returns `1`
    - `grep -c "onNodeSelect={selectNode}" app/tool/page.tsx` returns `1`
    - `grep -c "function simulateGraph" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "function nodeColor" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "svgRef" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "panStartRef" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "toGraphCoords" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "exportGraphPng" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "VIEW_WIDTH" app/tool/page.tsx` returns `0` (deleted)
    - `grep -c "<svg " app/tool/page.tsx` returns `0` (the inline SVG block is gone)
    - `grep -c "selectedLinkId" app/tool/page.tsx` returns >=2 (state declaration + evidence panel read — KEEPING is required)
    - `grep -c "visibleNodes" app/tool/page.tsx` returns >=2 (memo + prop usage — KEEPING required)
    - `grep -c 'disabled' app/tool/page.tsx` shows the Export PNG button is disabled (the disabled attribute appears on the button)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npm run test:contract` exits 0 (no pipeline regression)
    - `npx next lint` exits 0 OR is not a regression vs the previous lint baseline
  </acceptance_criteria>
  <done>app/tool/page.tsx renders <SemanticGraph> with the correct props; all SVG-only state, helpers, and the rAF simulation loop are deleted; selectedLinkId/visibleNodes/visibleLinks and surrounding panels remain intact; tsc and lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual /tool smoke test (VIS-01, VIS-02, VIS-03)</name>
  <files>app/tool/page.tsx (no edits — verify only)</files>
  <action>
    Pause execution and prompt the user to run the manual smoke test below in a real browser. The executor agent does NOT modify any files in this task; this checkpoint exists to confirm the in-browser behavior that vitest cannot test (per VALIDATION.md — sigma WebGL rendering requires a browser context).
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — automated verify is replaced by user-confirmed smoke test below"</automated>
  </verify>
  <done>User types "approved" after completing all 10 smoke-test steps in how-to-verify, OR reports the specific failure (e.g. "fa2-worker-failed") so a gap-closure plan can branch.</done>
  <what-built>
    Sigma-based WebGL graph renderer is now mounted on `/tool`. Entity-type fill colors, Louvain community rings, ForceAtlas2 worker layout, and node/stage click selection are wired. Pan and zoom are native sigma camera controls. Show-only-gaps filter is preserved.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (or restart dev server if already running so the new dependencies load).
    2. Sign in and visit http://localhost:3000/tool.
    3. Run an analysis with a brand likely to produce a medium graph (e.g. "Aesop" or any brand from your existing test inputs). Wait for the stream to complete.
    4. **VIS-01 (performance):**
       - Drag-pan the canvas and verify motion is smooth (no jank).
       - Scroll-zoom in and out; verify no dropped frames.
       - If your run produces a large graph (~50+ nodes), open DevTools Performance tab and record a 5-second drag/zoom session — verify the main thread is not blocked during ForceAtlas2 layout (worker thread should show the activity).
    5. **VIS-02 (visual encoding):**
       - Inspect the rendered graph. Confirm that nodes have at least 3-5 different fill colors corresponding to entity types (Person blue, Org teal, Concept violet, Event amber, Claim rose). If your sample graph happens to only emit one entityType, run a different brand input.
       - Confirm edge thickness varies: causal edges visibly thicker than associative edges; contextual edges appear faint/transparent. Missing/gap edges still render in rose-red `#fb7185`.
    6. **VIS-03 (community rings):**
       - On the same medium-to-large graph, look for colored border rings around groups of nodes (orange, cyan, lime, pink, etc.). They should appear ~6 seconds after the graph loads (after FA2 stabilizes and Louvain runs).
       - Run a second analysis on a query that should produce a small/simple graph. Confirm that on the simpler graph (likely <3 communities), the rings are absent. If you cannot reproduce a <3-community case, note this and we accept based on the unit-tested guard.
    7. **Selection wiring (D-14):**
       - Click a node — the evidence panel on the right updates to that node's content. The clicked node gets a bright near-white border (`#f8fafc`).
       - Click empty stage — selection clears (evidence panel returns to default state).
    8. **Show only gaps toggle (D-15):**
       - Toggle "Show only gaps" on. Verify only the gap edges remain visible. Toggle off — full graph returns.
    9. **Console hygiene:**
       - Open DevTools Console. Confirm no `[semantic-graph]` errors, no React hydration warnings, no `window is not defined`, no `UsageGraphError`, no FA2 worker URL errors.
       - If you see `[semantic-graph] FA2 worker failed` (RESEARCH.md Open Question 2 — Next.js 16.1.6 Turbopack blob URL bug), capture the error text. The graph should still render with a static (random) layout in that case.
    10. **Export PNG button (D-17):**
        - Confirm the Export PNG button is visibly grayed out and not clickable. Hover tooltip should read "PNG export returns in Phase 7."
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 checks pass. Otherwise describe each failing check verbatim. If FA2 worker fails (check 9, RESEARCH.md MEDIUM-risk Open Question 2), say "fa2-worker-failed" and we will branch to a sync-FA2 fallback gap-closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> /tool render | brand input flows through pipeline; SemanticGraph only receives already-validated GraphNode/GraphLink data (Plan 01 whitelisted entityType/relType) |
| sigma canvas -> DOM | WebGL canvas is the only DOM surface added; sigma manages its own event surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | app/tool/page.tsx render | mitigate | Component receives props only; no direct LLM string ever lands in render. Plan 01 whitelist + Plan 02 fallback colors are the layered defense. |
| T-06-07 | Information disclosure | DevTools console errors | accept | `[semantic-graph]` console logs may include sigma internal errors; logs contain no PII or secrets — graph data is already user-visible. Acceptable per existing logging convention (`[waitlist]`, `[stripe webhook]` precedents in CLAUDE.md). |
| T-06-08 | Denial of Service | FA2 worker on long-running tab | mitigate | Plan 02 task already implements the cleanup function; this plan inherits the mitigation by virtue of using `<SemanticGraph>` rather than re-implementing. |
</threat_model>

<verification>
- All grep-based acceptance criteria in Task 1 match
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `npm run test:contract` exits 0 (no pipeline regression)
- `npx next lint` exits 0 (or no new warnings vs baseline)
- Manual /tool smoke test (Task 2) passes all 10 checks; tester types "approved"
- `git diff --name-only` shows only `app/tool/page.tsx` modified in this plan
</verification>

<success_criteria>
- VIS-01: 200+ node graph renders smoothly under pan/zoom; FA2 runs off main thread
- VIS-02: At least 5 entity-type colors visible across runs; at least 3 edge styles visible (causal thick, associative medium, contextual faint, plus existing missing red)
- VIS-03: Louvain border rings visible on medium-to-large graphs; absent on simple graphs
- D-14 preserved: node click updates selectedNodeId; stage click clears
- D-15 preserved: showGapsOnly toggle filters edges as before
- D-13 preserved: pan/zoom work without custom code
- D-17 honored: Export PNG button is disabled placeholder
- Zero pipeline regression (test:contract green)
- No SVG-only state remains in app/tool/page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/06-graph-visualization-upgrade/06-03-SUMMARY.md` per the SUMMARY template. Include in the summary: (a) the result of the manual smoke test for each VIS-XX requirement, (b) whether the FA2 worker initialized successfully on Next.js 16.1.6 Turbopack (RESEARCH.md Open Question 2), and (c) the final line count of `app/tool/page.tsx` (delta vs pre-Plan-03 baseline).
</output>
