---
phase: 03-pipeline-extraction-scorer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - package.json
  - package-lock.json
  - lib/pipeline/scorer.ts
  - lib/pipeline/__tests__/scorer.test.ts
  - app/api/semantic-universe/route.ts
autonomous: true
requirements: [PIPE-01, PIPE-02]
must_haves:
  truths:
    - "`lib/pipeline/scorer.ts` reranks merged `SourceItem[]` with voyage **`rerank-2.5`** when `VOYAGE_API_KEY` present"
    - "Missing key / timeout / API error falls back to **identical ordering** to pre-scorer slice semantics"
    - "Synthesis receives **≤18** sources after scoring stage"
---

<objective>
Introduce **PIPE-02** voyage-powered reranking **between** retrieval and OpenAI synthesis. Package **`voyageai`** installed; scorer module unit-tested with mocked client; route inserts scorer call after `retrieveSourcesForBrand` and before synthesis.

Depends on Plan **03-01** (`models.ts`, `retriever.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-pipeline-extraction-scorer/03-CONTEXT.md
@.planning/research/STACK.md
@lib/pipeline/retriever.ts
@app/api/semantic-universe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install voyageai SDK</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `.planning/STATE.md` approved dependency note for `voyageai`
  </read_first>
  <action>
    Run `npm install voyageai` (pin caret range consistent with STACK.md `^0.2` if compatible — otherwise latest compatible and document in SUMMARY).

    Append to `.env.example`:
    ```
    # Voyage AI reranker (Phase 3 scorer) — optional; omit to skip reranking
    # VOYAGE_API_KEY=
    ```
  </action>
  <verify>
    <automated>node -e "require('./package.json').dependencies.voyageai || process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q voyageai package.json`
    - `grep -q VOYAGE_API_KEY .env.example`
  </acceptance_criteria>
  <done>Dependency available for scorer implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lib/pipeline/scorer.ts</name>
  <files>lib/pipeline/scorer.ts</files>
  <read_first>
    - node_modules voyageai README or TypeScript definitions (`Client`, rerank method names)
    - lib/pipeline/models.ts (`SourceItem`)
  </read_first>
  <action>
    Export:
    ```ts
    export async function scoreSourcesForSynthesis(
      sources: SourceItem[],
      brand: string,
      opts?: { voyageApiKey?: string; timeoutMs?: number; topN?: number },
    ): Promise<SourceItem[]>;
    ```

    Behavior:
    - Default **`topN`** = **18** (match current slice).
    - **Query string** for rerank: use `` `${brand} semantic brand universe competitive intent` `` (document constant `RERANK_QUERY_SUFFIX`).
    - If `!opts?.voyageApiKey?.trim()` → return `sources.slice(0, topN)` **without mutating** dedupe order beyond truncation.
    - If key present → call Voyage rerank API with model **`rerank-2.5`** (or SDK constant). Map ranked indices back to `SourceItem` objects. Preserve stable ordering for ties.
    - Wrap API call with **`AbortSignal.timeout(timeoutMs ?? 8000)`** or manual `Promise.race` — on failure log `[scorer]` warning and fall back to naive slice.

    Do **not** add network I/O besides Voyage when key present.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "scoreSourcesForSynthesis" lib/pipeline/scorer.ts`
    - `grep -q "rerank-2.5" lib/pipeline/scorer.ts`
    - `grep -q "8000" lib/pipeline/scorer.ts`
  </acceptance_criteria>
  <done>Scorer encapsulated with deterministic fallback path.</done>
</task>

<task type="auto">
  <name>Task 3: Wire scorer into semantic-universe stream (before synthesis)</name>
  <files>app/api/semantic-universe/route.ts</files>
  <read_first>
    - Current stream segment between `retrieveSourcesForBrand` completion and `synthesizeWithOpenAI` invocation
  </read_first>
  <action>
    1. Read `VOYAGE_API_KEY` via existing `cleanEnvValue` pattern at POST scope; pass into stream closure.

    2. After retrieving `sources`, assign:
       ```ts
       const rankedSources = await scoreSourcesForSynthesis(sources, brand, {
         voyageApiKey: voyageKey,
         timeoutMs: 8000,
         topN: 18,
       });
       ```

    3. Pass **`rankedSources`** (not `sources`) into `extractCompetitorNamesFromSources`, `synthesizeWithOpenAI`, and `enrichVisualCorrelationsWithImages`.

    4. Keep NDJSON detail strings semantically equivalent (`${sources.length}` may remain based on raw retrieval count OR switch to ranked length — choose **ranked length** for `sources` step done message only if user-visible copy stays understandable; prefer keeping **two counts** in detail: `collected X, reranked Y` if necessary — Claude's discretion but must not break client parsers assuming string exists).

    Minimal requirement: `detail` remains a non-empty human-readable string when status `done`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "scoreSourcesForSynthesis" app/api/semantic-universe/route.ts`
    - `grep -q "rankedSources" app/api/semantic-universe/route.ts`
  </acceptance_criteria>
  <done>Voyage rerank sits on critical path with safe fallback.</done>
</task>

<task type="auto">
  <name>Task 4: Vitest tests for scorer fallback + ordering</name>
  <files>lib/pipeline/__tests__/scorer.test.ts</files>
  <read_first>
    - lib/pipeline/scorer.ts
  </read_first>
  <action>
    Cover:
    - No API key → output equals input truncated to `topN`.
    - Mock voyage client success path reorders items deterministically (inject dependency **optional** — if SDK hard to mock, mock `fetch` global if SDK uses fetch internally; otherwise export inner rerank function for testing).

    At least **2** tests.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/scorer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/pipeline/__tests__/scorer.test.ts`
    - `npm test` exits 0
  </acceptance_criteria>
  <done>PIPE-02 behavior guarded by automated tests.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-03-03 | Denial of Service | Hard timeout + fallback prevents hung scorer |
| T-03-04 | Information Disclosure | Never log full API key — bracketed `[scorer]` logs redact |
</threat_model>

<verification>
- `npm test`
- Manual optional: set `VOYAGE_API_KEY` locally — confirm rerank path hits network once
</verification>

<success_criteria>
- Roadmap SC3 satisfied: merged Tavily+Exa results pass through voyage rerank before OpenAI when configured.
</success_criteria>
