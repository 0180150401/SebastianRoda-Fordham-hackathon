---
phase: 03-pipeline-extraction-scorer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pipeline/models.ts
  - lib/pipeline/retriever.ts
  - lib/pipeline/__tests__/retriever.test.ts
  - app/api/semantic-universe/route.ts
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "`lib/pipeline/models.ts` exports domain types previously declared only inside `route.ts` (`SourceItem`, graph/evidence/discourse types, `SemanticUniversePayload`, `TRACKED_MODELS`)"
    - "`lib/pipeline/retriever.ts` exports `retrieveSourcesForBrand(brand, { tavilyKey, exaKey })` returning merged deduped sources identical to pre-refactor ordering semantics"
    - "`app/api/semantic-universe/route.ts` imports retriever + models — duplicate inline definitions removed"
  artifacts:
    - path: "lib/pipeline/retriever.ts"
      provides: "Parity Tavily+Exa retrieval"
      min_lines: 40
---

<objective>
Extract **shared domain types** and **retrieval I/O** from `app/api/semantic-universe/route.ts` into `lib/pipeline/models.ts` and `lib/pipeline/retriever.ts` without changing merge/dedupe semantics or Tavily/Exa HTTP payloads. Preserve Phase 2 telemetry + stream outer shell untouched aside from import rewires.

This plan intentionally **does not** extract synthesis, scoring, or enrichment — those land in Plans 03-02/03-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-pipeline-extraction-scorer/03-CONTEXT.md
@app/api/semantic-universe/route.ts
@lib/pipeline/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/pipeline/models.ts — domain types + constants</name>
  <files>lib/pipeline/models.ts, app/api/semantic-universe/route.ts</files>
  <read_first>
    - app/api/semantic-universe/route.ts (types `GraphNode` through `SemanticUniversePayload`, `TRACKED_MODELS`)
    - lib/pipeline/types.ts (wire events only — do not rename or merge modules)
  </read_first>
  <action>
    1. Create `lib/pipeline/models.ts` exporting **exact** TypeScript types and `TRACKED_MODELS` moved verbatim from the route (switch `type` → `export type`, `interface` → `export interface` as needed).

    2. Update `app/api/semantic-universe/route.ts` to `import type { … } from "@/lib/pipeline/models"` (and value import for `TRACKED_MODELS`).

    3. Remove the duplicate type blocks from the route file **without** altering any runtime logic in this task.

    **Forbidden:** touching `PipelineEvent` / zod schemas in `lib/pipeline/types.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/pipeline/models.ts`
    - `grep -q "export.*SourceItem" lib/pipeline/models.ts`
    - `grep -q '@/lib/pipeline/models' app/api/semantic-universe/route.ts`
    - Route file **does not** declare `type SourceItem =` locally (`grep -c "^type SourceItem" app/api/semantic-universe/route.ts` → `0`)
  </acceptance_criteria>
  <done>Single source of truth for pipeline domain types lives under `lib/pipeline/models.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/pipeline/retriever.ts + wire route</name>
  <files>lib/pipeline/retriever.ts, app/api/semantic-universe/route.ts</files>
  <read_first>
    - app/api/semantic-universe/route.ts (`dedupeByUrl`, `fetchTavily`, `fetchExa` implementations)
    - `.planning/phases/03-pipeline-extraction-scorer/03-CONTEXT.md` D-01
  </read_first>
  <action>
    1. Move **`dedupeByUrl`**, **`fetchTavily`**, **`fetchExa`** into `lib/pipeline/retriever.ts`, importing `SourceItem` from `./models`.

    2. Export **`dedupeByUrl`** (named export) for reuse by Plan 03-03 enricher.

    3. Export async function:
       ```ts
       export async function retrieveSourcesForBrand(
         brand: string,
         opts: { tavilyKey: string; exaKey: string },
       ): Promise<SourceItem[]>;
       ```
       Implementation must preserve today’s behavior: `Promise.all([fetchTavily, fetchExa])`, `.catch(() => [])`, then `dedupeByUrl([...]).slice(0, 18)` **exactly as currently ordered in route stream**.

    4. Replace inline bodies inside route stream with:
       ```ts
       const sources = await retrieveSourcesForBrand(brand, { tavilyKey, exaKey });
       ```

    5. Delete old helper definitions from route after imports compile.

    **Pass-through:** keep Tavily/Exa URLs, headers, JSON shapes identical — copy/paste bodies if needed to avoid drift.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function dedupeByUrl" lib/pipeline/retriever.ts`
    - `grep -q "retrieveSourcesForBrand" app/api/semantic-universe/route.ts`
    - `grep -q "fetchTavily" lib/pipeline/retriever.ts`
    - `grep -q "fetchExa" lib/pipeline/retriever.ts`
    - Route file no longer defines `function dedupeByUrl` (`grep -c "function dedupeByUrl" app/api/semantic-universe/route.ts` → `0`)
  </acceptance_criteria>
  <done>Retrieval stage isolated and importable without Next route instantiation.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest unit tests for dedupe + retriever (mocked fetch)</name>
  <files>lib/pipeline/__tests__/retriever.test.ts</files>
  <read_first>
    - lib/pipeline/retriever.ts
    - Existing Vitest patterns under `lib/pipeline/__tests__/`
  </read_first>
  <action>
    Create `lib/pipeline/__tests__/retriever.test.ts` covering:
    1. **`dedupeByUrl`** collapses duplicate URLs (two items same `url` → one remains).
    2. **`retrieveSourcesForBrand`** — stub global `fetch` to return minimal JSON matching Tavily/Exa response parsing branches; assert merged array length cap behavior.

    Use `vi.stubGlobal("fetch", …)` and restore in `afterEach`.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/retriever.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/pipeline/__tests__/retriever.test.ts`
    - File contains `describe("retrieveSourcesForBrand"` substring
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Retriever meets roadmap SC2 testability gate for Layer 2.</done>
</task>

</tasks>

<threat_model>
## STRIDE (ASVS L1)

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-03-01 | Tampering | Retrieval helpers accept only server-supplied keys — no new client-facing surface |
| T-03-02 | Information Disclosure | Tests must not embed real API keys — mocked fetch only |
</threat_model>

<verification>
- `npm test`
- `npx tsc --noEmit`
</verification>

<success_criteria>
- Retrieval logic lives in `lib/pipeline/retriever.ts` with Vitest coverage; route stream still emits identical step timings labels (`sources` stage).
</success_criteria>
