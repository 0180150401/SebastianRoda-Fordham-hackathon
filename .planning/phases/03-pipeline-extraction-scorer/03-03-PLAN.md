---
phase: 03-pipeline-extraction-scorer
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - lib/pipeline/enricher.ts
  - lib/pipeline/structurer.ts
  - lib/pipeline/stream-handler.ts
  - app/api/semantic-universe/route.ts
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "`lib/pipeline/enricher.ts` owns image fetch helpers + `enrichVisualCorrelationsWithImages`"
    - "`lib/pipeline/structurer.ts` owns OpenAI synthesis + `buildFallback` + normalization helper DAG feeding them"
    - "`lib/pipeline/stream-handler.ts` runs NDJSON stream stages (run_meta → steps → done/error) using modules from Plans 03-01/02 + structurer/enricher"
    - "`export async function POST` in `route.ts` is ≤ **65** physical lines (including blanks)"
---

<objective>
Finish PIPE-01 decomposition: relocate **image enrichment** and **LLM structuring/fallback** out of `route.ts`, centralize **stream orchestration** in `lib/pipeline/stream-handler.ts`, and leave `app/api/semantic-universe/route.ts` as an auth/budget/telemetry shell around `new ReadableStream(...)`.

Depends on **03-01** (models/retriever) and **03-02** (scorer wiring).

Success equals roadmap SC1–SC4: modular imports, POST coordinator ≤65 lines, rerank remains before synthesis (from 03-02), contract tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-pipeline-extraction-scorer/03-CONTEXT.md
@app/api/semantic-universe/route.ts
@lib/pipeline/models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract enricher.ts (image pipeline)</name>
  <files>lib/pipeline/enricher.ts, app/api/semantic-universe/route.ts</files>
  <read_first>
    - Route helpers from `looksLikeImageUrl` through `enrichVisualCorrelationsWithImages` inclusive (currently located between dedupe migration boundary — ensure no duplicate definitions remain after Plan 03-01)
    - `lib/pipeline/retriever.ts` exports `dedupeByUrl` **or** import dedupe from retriever — enricher should `import { dedupeByUrl } from "@/lib/pipeline/retriever"` if exporter adds named export (add `export` to `dedupeByUrl` in retriever when needed)
  </read_first>
  <action>
    1. Create `lib/pipeline/enricher.ts` exporting `enrichVisualCorrelationsWithImages(payload, sources)` plus private helpers (`resolveSourceImages`, meta parsers, etc.) moved verbatim.

    2. Export **`dedupeByUrl`** from `retriever.ts` if not already exported — enricher imports it.

    3. Remove moved functions from route; route/stream-handler imports enricher API later.

    **Do not** relocate synthesis functions in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enrichVisualCorrelationsWithImages" lib/pipeline/enricher.ts`
    - Route file does not define `async function enrichVisualCorrelationsWithImages` (`grep -c "async function enrichVisualCorrelationsWithImages" app/api/semantic-universe/route.ts` → `0`)
  </acceptance_criteria>
  <done>Image enrichment isolated per ARCHITECTURE Layer 5.</done>
</task>

<task type="auto">
  <name>Task 2: Extract structurer.ts (synthesis + fallback + normalization)</name>
  <files>lib/pipeline/structurer.ts, app/api/semantic-universe/route.ts</files>
  <read_first>
    - Full helper closure feeding `synthesizeWithOpenAI` (starts near `extractJsonObject`, ends at closing brace of `synthesizeWithOpenAI` — includes `buildFallback`, `normalizeModelPayload`, competitor/evidence utilities exclusively consumed there)
    - `OpenAI` import usage — structurer accepts `openai: OpenAI` parameter just like today
  </read_first>
  <action>
    1. Create `lib/pipeline/structurer.ts` exporting at minimum:
       - `synthesizeWithOpenAI(...)` **same return shape** `{ payload, usage }` as Phase 2 implementation
       - `buildFallback(...)`, `extractCompetitorNamesFromSources(...)`

    2. Move **all** dependent pure/helpers that are not used by enricher or retriever into structurer module (or split `lib/pipeline/graph-helpers.ts` if circular risk — prefer single file unless >800 lines forces split).

    3. Update route (temporary) to import these exports — **Task 3** will relocate calls into stream-handler.

    4. Preserve identical prompts / JSON schema instructions / model id strings — copy verbatim.

    After move, route must compile even before stream-handler extraction (temporary imports acceptable mid-task).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "synthesizeWithOpenAI" lib/pipeline/structurer.ts`
    - Route references structurer via `@/lib/pipeline/structurer` **or** via stream-handler only after Task 3 — final state must not duplicate `function synthesizeWithOpenAI` in route (`grep -c "async function synthesizeWithOpenAI" app/api/semantic-universe/route.ts` → `0`)
  </acceptance_criteria>
  <done>LLM structuring isolated for independent testing.</done>
</task>

<task type="auto">
  <name>Task 3: Create stream-handler.ts & slim route.ts</name>
  <files>lib/pipeline/stream-handler.ts, app/api/semantic-universe/route.ts</files>
  <read_first>
    - Current `ReadableStream` constructor block inside POST (run_meta through telemetry insert) — entire `start` callback body
    - Phase 2 telemetry requirements (`semantic_pipeline_runs`, `applyOpenAiUsage`) — remain invoked from handler **or** thin callbacks passed via deps object
  </read_first>
  <action>
    1. Add `lib/pipeline/stream-handler.ts` exporting async function, e.g. `runSemanticUniverseAnalysisStream(args)` where `args` includes:
       - `controller`, `encoder`
       - `runId`, `brand`, `user`, `supabase`
       - API keys (`openAiKey`, `tavilyKey`, `exaKey`, optional `voyageKey`)
       - `openai` traced client
       - subscription/demo flags needed for `markFreeDemoUsed` + cookie behavior (pass booleans + user id)
       - **`emitTelemetry`** hooks or direct imports from route-level utilities — simplest: pass `markFreeDemoUsed` function reference + import telemetry helpers from `@/lib/usage/daily-token-budget` inside handler (allowed) and `createAdminClient` usage stays out of handler if possible — acceptable to import server helpers when needed.

    2. Move **`emitLine`** helper next to stream handler (same file) — signature `(controller, encoder, event)`.

    3. Handler orchestrates:
       - `emit run_meta`
       - sources stage timings using `retrieveSourcesForBrand`
       - scorer `scoreSourcesForSynthesis`
       - synthesis via structurer imports
       - enrichment via enricher import
       - demo marking + DB inserts exactly as current ordering

    4. Replace route `start` body with single call into handler (≤3 statements inside start).

    5. **POST shell** after refactor: auth/kill switch/budget/profile/OpenAI client creation/runId/stream/response cookies — **`export async function POST` must be ≤ 65 lines** end-to-end (count manually or with editor fold — record count in plan SUMMARY).

    6. Final `route.ts` total lines should drop materially versus ~1 750-line baseline (exact target left to executor judgment once POST gate satisfied).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/pipeline/stream-handler.ts`
    - `grep -q "runSemanticUniverseAnalysisStream\\|attachSemanticUniverseStream" lib/pipeline/stream-handler.ts` (name chosen by implementer — grep pattern matches exported runner symbol documented in SUMMARY)
    - `grep -c "async function synthesizeWithOpenAI" app/api/semantic-universe/route.ts` → `0`
    - `grep -c "async function enrichVisualCorrelationsWithImages" app/api/semantic-universe/route.ts` → `0`
    - Contract suite green (`npm test`)
  </acceptance_criteria>
  <done>Route becomes coordinator; modules individually importable.</done>
</task>

<task type="auto">
  <name>Task 4: Minimal structurer unit smoke test</name>
  <files>lib/pipeline/__tests__/structurer.test.ts</files>
  <read_first>
    - lib/pipeline/structurer.ts exports worth testing (`extractJsonObject` if exported OR test via small exported pure helper)
  </read_first>
  <action>
    Add **≥1** deterministic unit test that does **not** call OpenAI network — e.g. test `extractJsonObject`/`buildFallback` invariant on toy input by exporting tiny helper **or** mocking OpenAI client.

    Goal: roadmap SC2 satisfied with **meaningful** automated coverage beyond retriever/scorer tests.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/structurer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/pipeline/__tests__/structurer.test.ts`
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Structurer module independently verifiable.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-03-05 | Tampering | Stream handler receives server-side deps only — no new user-controlled imports |
| T-03-06 | Denial of Service | Preserve Phase 2 timeouts / budgets outside handler |
</threat_model>

<verification>
- `npm test` (must include Phase 1 NDJSON contract suite untouched)
- Optional manual `/tool` smoke — graph renders, steps progress
</verification>

<success_criteria>
- Roadmap SC1 **thin coordinator** achieved with modular pipeline files.
- SC4 contract regression suite passes unchanged aside from intentional import path adjustments.
</success_criteria>
