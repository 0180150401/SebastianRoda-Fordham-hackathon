---
phase: 04-multi-stage-retrieval-query-planner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pipeline/query-planner.ts
  - lib/pipeline/__tests__/query-planner.test.ts
  - .env.example
autonomous: true
requirements: [RTRY-01, RTRY-02]
must_haves:
  truths:
    - "D-01: Planner returns structured query objects, not strings"
    - "D-02: Planner enforces adaptive 5-12 query objects per run"
    - "D-03: Query objects cover the competitive universe categories"
    - "D-04: Planner includes 1-2 bounded recency queries"
    - "D-17: Defaults enforce 5-12 query objects, max 8 provider searches per provider, 8s provider timeout, and 45s retrieval budget"
    - "D-18: Limits are environment-configurable with safe defaults"
    - "D-19: Planner performs one pass only; no adaptive rescue loop"
  artifacts:
    - path: "lib/pipeline/query-planner.ts"
      provides: "Structured query planner and limits helpers"
      min_lines: 80
---

<objective>
Create the Phase 4 query planner module: typed retrieval query objects, strict parsing/fallback behavior, coverage-first category selection, bounded recency, and conservative retrieval limits. This plan does not execute provider fanout; it produces the stable plan object consumed by later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-multi-stage-retrieval-query-planner/04-CONTEXT.md
@.planning/phases/04-multi-stage-retrieval-query-planner/04-RESEARCH.md
@lib/pipeline/types.ts
@lib/openai.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement retrieval limits helper</name>
  <files>lib/pipeline/query-planner.ts, .env.example</files>
  <read_first>
    - 04-CONTEXT.md D-17 and D-18
    - Existing env parsing patterns in `app/api/semantic-universe/route.ts`
  </read_first>
  <action>
    Create `lib/pipeline/query-planner.ts` with exported constants and helper:
    ```ts
    export type RetrievalLimits = {
      minQueries: number;
      maxQueries: number;
      maxProviderSearchesPerProvider: number;
      providerTimeoutMs: number;
      totalRetrievalBudgetMs: number;
      minimumUsableSources: number;
    };

    export function getRetrievalLimits(env?: NodeJS.ProcessEnv): RetrievalLimits;
    ```

    Defaults:
    - min queries: 5
    - max queries: 12
    - max provider searches per provider: 8
    - provider timeout: 8000 ms
    - total retrieval budget: 45000 ms
    - minimum usable sources: 6

    Add documented optional env vars to `.env.example` for these knobs. Clamp invalid env values back to safe defaults.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'getRetrievalLimits' lib/pipeline/query-planner.ts`
    - `grep -q 'SEMANTIC_RETRIEVAL_MAX_QUERIES' .env.example`
    - Tests later prove invalid env values do not escape caps
  </acceptance_criteria>
  <done>Phase 4 production caps are centralized and testable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured planner with OpenAI + deterministic fallback</name>
  <files>lib/pipeline/query-planner.ts</files>
  <read_first>
    - 04-RESEARCH.md OpenAI structured outputs guidance
    - lib/pipeline/types.ts query object schema from Plan 04-01
    - Existing OpenAI client/tracing patterns
  </read_first>
  <action>
    Export:
    ```ts
    export async function planRetrievalQueries(
      brand: string,
      opts: {
        openaiApiKey?: string;
        runId?: string;
        limits?: RetrievalLimits;
      },
    ): Promise<QueryPlanEvent>;
    ```

    Behavior:
    - Prefer OpenAI structured output/Zod parsing when `openaiApiKey` is present.
    - Validate every output object against the shared query-plan schema.
    - If OpenAI is unavailable, invalid, or outside caps, return a deterministic fallback plan.
    - Every query object must include the brand in `searchPhrase` and success criteria.
    - Use category spread from D-03 and 1-2 bounded recency query objects from D-04.
    - Include sanitized `display` copy that is friendly and does not center raw search syntax.

    The fallback plan should normally generate 8 queries and stay within provider caps.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'planRetrievalQueries' lib/pipeline/query-planner.ts`
    - `grep -q 'competitors' lib/pipeline/query-planner.ts`
    - `grep -q 'recent_signals' lib/pipeline/query-planner.ts`
    - `grep -q 'fallback' lib/pipeline/query-planner.ts`
  </acceptance_criteria>
  <done>A stable query plan can be produced without provider retrieval.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-test planner caps, coverage, recency, fallback</name>
  <files>lib/pipeline/__tests__/query-planner.test.ts</files>
  <read_first>
    - lib/pipeline/query-planner.ts
    - lib/pipeline/types.ts
    - Existing Vitest patterns under `lib/pipeline/__tests__/`
  </read_first>
  <action>
    Add tests covering:
    - default limits equal the locked Phase 4 caps
    - invalid env overrides are clamped or ignored
    - fallback plan returns between 5 and 12 query objects
    - fallback includes competitive-universe categories and 1-2 bounded recency queries
    - every query includes the brand and provider hints stay within caps
    - malformed OpenAI output falls back instead of throwing malformed stream data

    Mock OpenAI; no live network calls.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/query-planner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/pipeline/__tests__/query-planner.test.ts`
    - Test file contains `fallback`
    - Test file contains `bounded`
  </acceptance_criteria>
  <done>Planner behavior is deterministic enough to execute in later plans.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-04-03 | Denial of Service | Hard min/max query and provider caps prevent unbounded fanout. |
| T-04-04 | Tampering | Invalid or malformed LLM output is schema-rejected and replaced with deterministic fallback. |
| T-04-09 | Information Disclosure | Planner logs must not include API keys or full raw provider request payloads. |
</threat_model>

<verification>
- `npx vitest run lib/pipeline/__tests__/query-planner.test.ts`
- `npm test`
- `npx tsc --noEmit`
</verification>

<success_criteria>
RTRY-01 and RTRY-02 are ready for execution: the system can generate a deliberate, typed, user-displayable retrieval plan inside conservative production caps.
</success_criteria>
