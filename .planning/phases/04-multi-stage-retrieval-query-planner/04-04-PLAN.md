---
phase: 04-multi-stage-retrieval-query-planner
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - lib/pipeline/stream-handler.ts
  - lib/pipeline/__tests__/stream-handler.test.ts
  - app/api/semantic-universe/route.ts
  - supabase/migrations/20260506000000_phase4_retrieval_stats.sql
autonomous: true
requirements: [RTRY-01, RTRY-02]
must_haves:
  truths:
    - "D-05: Stream emits `query_plan` before retrieval begins"
    - "D-08: Sources progress uses aggregate counts, not per-query progress"
    - "D-10: Retrieval degrades only below deterministic minimum viable evidence"
    - "D-11: Thin evidence continues as degraded with visible reason where possible"
    - "D-12: Failure visibility is recorded in telemetry plus concise stream detail"
    - "D-14: Quality filtering happens before Voyage scoring"
    - "D-19: Integration runs one planned fanout pass only"
  artifacts:
    - path: "lib/pipeline/stream-handler.ts"
      provides: "Planner/retriever/filter/scorer orchestration"
      min_lines: 120
---

<objective>
Integrate the Phase 4 planner, planned retrieval fanout, source-quality filter, aggregate stream details, degraded retrieval behavior, and additive telemetry persistence into the semantic-universe stream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-multi-stage-retrieval-query-planner/04-CONTEXT.md
@.planning/phases/04-multi-stage-retrieval-query-planner/04-RESEARCH.md
@lib/pipeline/stream-handler.ts
@lib/pipeline/query-planner.ts
@lib/pipeline/retriever.ts
@lib/pipeline/source-quality.ts
@lib/pipeline/scorer.ts
@app/api/semantic-universe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit query_plan before sources retrieval</name>
  <files>lib/pipeline/stream-handler.ts, app/api/semantic-universe/route.ts</files>
  <read_first>
    - lib/pipeline/stream-handler.ts existing event order
    - app/api/semantic-universe/route.ts env/key handoff
    - 04-01-PLAN.md event contract
  </read_first>
  <action>
    Wire `planRetrievalQueries` into `runSemanticUniverseAnalysisStream` immediately after `run_meta` and before the `sources` step starts.

    Pass the OpenAI API key and retrieval limits from the route/env as needed. Emit the returned `query_plan` event through the existing typed `emitLine` path before any Tavily/Exa fetch begins.

    Event order must be:
    `run_meta` -> `query_plan` -> `sources running` -> `sources done` -> `synthesis` -> `images` -> `done/error`.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/stream-handler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'planRetrievalQueries' lib/pipeline/stream-handler.ts`
    - `grep -q 'query_plan' lib/pipeline/stream-handler.ts`
    - Test asserts `query_plan` occurs before `sources` running
  </acceptance_criteria>
  <done>The user sees the retrieval plan before retrieval begins.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate retrieval, quality filter, scorer, and degraded evidence</name>
  <files>lib/pipeline/stream-handler.ts, lib/pipeline/__tests__/stream-handler.test.ts</files>
  <read_first>
    - lib/pipeline/retriever.ts new return shape
    - lib/pipeline/source-quality.ts
    - lib/pipeline/scorer.ts
    - 04-CONTEXT.md D-10 through D-12
  </read_first>
  <action>
    Replace the simple retrieval/scoring sequence with:
    1. planned retrieval fanout using the emitted query plan
    2. merge/dedupe already performed by retriever
    3. `filterSourcesForQuality`
    4. minimum viable evidence check using `limits.minimumUsableSources`
    5. `scoreSourcesForSynthesis(usableSources, ...)`
    6. synthesis with ranked usable sources

    If usable evidence is below threshold, continue with the usable sources when non-empty and set a degraded reason such as `thin_retrieval_evidence`. The `sources` done detail must include aggregate counts: planned, succeeded, failed, collected, filtered, usable, and degraded reason when present.

    Do not implement an adaptive retry/rescue loop.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/stream-handler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Stream-handler tests cover normal retrieval, one failed sub-query, and thin evidence degraded path
    - `grep -q 'filterSourcesForQuality' lib/pipeline/stream-handler.ts`
    - `grep -q 'thin_retrieval_evidence' lib/pipeline/stream-handler.ts`
  </acceptance_criteria>
  <done>Only usable evidence reaches scoring/synthesis and thin evidence is visible.</done>
</task>

<task type="manual">
  <name>Task 3: Add additive Supabase telemetry columns and apply migration</name>
  <files>supabase/migrations/20260506000000_phase4_retrieval_stats.sql</files>
  <read_first>
    - Existing Supabase migrations for `semantic_pipeline_runs`
    - .planning/phases/02-observability-security-cost-controls/02-01-PLAN.md
  </read_first>
  <action>
    Create an additive migration for `semantic_pipeline_runs` with nullable retrieval fields:
    - `retrieval_query_count integer`
    - `retrieval_provider_success_count integer`
    - `retrieval_provider_failure_count integer`
    - `retrieval_filtered_count integer`
    - `retrieval_degraded_reason text`
    - `retrieval_filter_reasons jsonb`

    Keep RLS unchanged unless existing migration style requires policy updates.

    [BLOCKING] Apply the migration to the target Supabase database before relying on telemetry persistence in production:
    `supabase db push`
  </action>
  <verify>
    <manual>`supabase db push` completes against the intended environment</manual>
    <automated>rg 'retrieval_query_count|retrieval_filter_reasons' supabase/migrations</automated>
  </verify>
  <acceptance_criteria>
    - Migration is additive and nullable
    - No destructive column drops or RLS relaxations
    - Production/staging schema push is recorded in phase verification
  </acceptance_criteria>
  <done>Telemetry has a durable place for query-plan and filter aggregate stats.</done>
</task>

<task type="auto">
  <name>Task 4: Persist retrieval stats and finish end-to-end tests</name>
  <files>lib/pipeline/stream-handler.ts, lib/pipeline/__tests__/stream-handler.test.ts</files>
  <read_first>
    - lib/pipeline/stream-handler.ts existing telemetry insert/update path
    - Supabase migration from Task 3
  </read_first>
  <action>
    Persist aggregate retrieval stats to the new nullable columns when the telemetry row exists. Do not fail the run if telemetry persistence fails; follow the existing Phase 2 telemetry error posture.

    Add or update stream-handler tests for:
    - event order
    - aggregate `sources` detail
    - partial failure still reaches synthesis
    - telemetry update payload includes retrieval counts and filter reason counts
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes
    - `npx tsc --noEmit` passes
    - Manual `/tool` smoke confirms query plan appears before retrieval and remains in trace
  </acceptance_criteria>
  <done>Phase 4 behavior is integrated, tested, and observable.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-04-07 | Repudiation | Retrieval plan/counts/filter reasons are persisted as telemetry for later audit. |
| T-04-08 | Denial of Service | No retry/rescue loop; integration uses the same hard caps and timeouts from planner/retriever. |
| T-04-11 | Information Disclosure | Stream detail exposes aggregate counts and degraded reason, not full rejected source bodies. |
</threat_model>

<verification>
- `npx vitest run lib/pipeline/__tests__/stream-handler.test.ts`
- `npm test`
- `npx tsc --noEmit`
- Manual: `/tool` smoke with a known brand confirms `query_plan` before retrieval and preserved trace after completion
- Manual/blocking before production: `supabase db push`
</verification>

<success_criteria>
Phase 4 is complete when the stream emits a visible retrieval plan before planned fanout, retrieval runs 5-12 targeted queries inside caps, low-quality sources are filtered before synthesis, partial provider failures continue, and aggregate retrieval stats are visible in stream detail and telemetry.
</success_criteria>
