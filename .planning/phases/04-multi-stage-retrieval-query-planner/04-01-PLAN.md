---
phase: 04-multi-stage-retrieval-query-planner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pipeline/types.ts
  - lib/pipeline/__tests__/pipeline.contract.test.ts
  - hooks/use-semantic-universe-stream.ts
  - app/tool/page.tsx
autonomous: true
requirements: [RTRY-02]
must_haves:
  truths:
    - "D-05: `query_plan` is a typed stream event emitted before retrieval begins"
    - "D-06: The client preserves the friendly query plan in the trace panel after completion"
    - "D-07: Event payload contains internal query objects plus sanitized display fields"
    - "D-08: Phase 4 does not add per-query progress events"
  artifacts:
    - path: "lib/pipeline/types.ts"
      provides: "Typed query_plan stream contract"
      min_lines: 80
---

<objective>
Extend the shared NDJSON stream contract with a typed `query_plan` event and make the `/tool` client preserve its friendly display payload in the existing trace/progress surface. This plan is contract-only: it does not yet generate or execute retrieval plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-multi-stage-retrieval-query-planner/04-CONTEXT.md
@.planning/phases/04-multi-stage-retrieval-query-planner/04-RESEARCH.md
@lib/pipeline/types.ts
@hooks/use-semantic-universe-stream.ts
@app/tool/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add query_plan event schema</name>
  <files>lib/pipeline/types.ts</files>
  <read_first>
    - lib/pipeline/types.ts (`pipelineEventSchema`, existing event shapes)
    - 04-CONTEXT.md D-05 through D-08
  </read_first>
  <action>
    Add zod schemas and exported TypeScript types for:
    - `RetrievalQueryObject`
    - `QueryPlanDisplay`
    - `QueryPlanEvent`

    Required event shape:
    ```ts
    {
      type: "query_plan",
      plan_id: string,
      display: {
        title: string,
        summary: string,
        items: Array<{ id: string; label: string; intent: string }>
      },
      queries: RetrievalQueryObject[]
    }
    ```

    `RetrievalQueryObject` must include id, label, intent, searchPhrase, category, recency, providers, and successCriteria. Categories must cover the Phase 4 research list; recency must be `"none" | "bounded"`; providers must be `"tavily" | "exa"`.

    Do not add a new step id. `query_plan` is an event, not a progress step.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'query_plan' lib/pipeline/types.ts`
    - `grep -q 'RetrievalQueryObject' lib/pipeline/types.ts`
    - `grep -q 'bounded' lib/pipeline/types.ts`
  </acceptance_criteria>
  <done>Stream contract accepts a typed retrieval plan event.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract fixtures for query_plan</name>
  <files>lib/pipeline/__tests__/pipeline.contract.test.ts</files>
  <read_first>
    - lib/pipeline/__tests__/pipeline.contract.test.ts
    - lib/pipeline/types.ts
  </read_first>
  <action>
    Add positive contract coverage for a valid `query_plan` event and negative coverage for at least one malformed plan event.

    The valid fixture must include:
    - at least two query objects
    - one bounded recency query
    - display items that do not expose raw provider syntax as the primary UI copy

    Keep existing event tests intact.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/pipeline.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Contract tests include the string `query_plan`
    - Malformed `query_plan` parsing fails
  </acceptance_criteria>
  <done>Strict NDJSON validation covers the new event before server emission exists.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve query plan in stream hook state</name>
  <files>hooks/use-semantic-universe-stream.ts</files>
  <read_first>
    - hooks/use-semantic-universe-stream.ts
    - app/tool/page.tsx trace consumption shape
  </read_first>
  <action>
    Extend hook state with an optional query-plan field using the exported `QueryPlanEvent`/display type.

    On `query_plan` events:
    - store the display payload and plan id
    - append/record the event through the same trace mechanism used for other parsed events, if one exists
    - do not mutate step status or add per-query progress rows

    Preserve fail-fast parsing behavior from Phase 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Hook imports or infers the query plan type from `lib/pipeline/types.ts`
    - Hook has a `case "query_plan"` or equivalent typed branch
  </acceptance_criteria>
  <done>The client can retain the retrieval plan without changing server behavior yet.</done>
</task>

<task type="auto">
  <name>Task 4: Render friendly plan summary in existing trace UI</name>
  <files>app/tool/page.tsx</files>
  <read_first>
    - app/tool/page.tsx
    - hooks/use-semantic-universe-stream.ts
  </read_first>
  <action>
    Surface the stored query plan in the existing trace/progress area using the display fields only. Show the title, summary, and item labels/intents in compact copy.

    Do not show raw `searchPhrase` as primary UI text. Do not add a new warning panel or full UI redesign.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'queryPlan' app/tool/page.tsx hooks/use-semantic-universe-stream.ts`
    - Query plan rendering references `display`
    - No per-query progress state is introduced
  </acceptance_criteria>
  <done>Users can inspect the friendly retrieval plan after the run completes.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-04-01 | Tampering | Zod validates every `query_plan` event; malformed events fail contract tests. |
| T-04-02 | Information Disclosure | UI renders sanitized display fields instead of making raw search syntax the primary copy. |
</threat_model>

<verification>
- `npx vitest run lib/pipeline/__tests__/pipeline.contract.test.ts`
- `npm test`
- `npx tsc --noEmit`
</verification>

<success_criteria>
RTRY-02 contract foundation is complete: the stream can carry a user-visible retrieval plan and the client can preserve it without weakening NDJSON validation.
</success_criteria>
