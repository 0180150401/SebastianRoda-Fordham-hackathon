---
phase: 05-graph-synthesis-quality-provenance
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - lib/pipeline/stream-handler.ts
  - lib/pipeline/types.ts
  - hooks/use-semantic-universe-stream.ts
  - app/tool/page.tsx
  - lib/pipeline/__tests__/pipeline.contract.test.ts
  - lib/pipeline/__tests__/stream-handler.test.ts
  - supabase/migrations/20260508060000_phase5_synthesis_provenance.sql
autonomous: false
requirements: [SYN-01, SYN-02]
requirements_addressed: [SYN-01, SYN-02]
must_haves:
  truths:
    - "D-07: Fallback/degraded synthesis is visible, not telemetry-only"
    - "D-08: `/tool` gets a small status indicator, not a full evidence panel"
    - "D-09: Payload and stream carry `result_type` plus concise reason"
    - "D-10: Every graph edge has at least one valid passage `evidenceId`"
    - "D-16: Degraded partial graphs are returned when usable floor still holds"
    - "D-17: Below the small usable floor, fallback is shown instead"
    - "D-18: Minimum usable structure is 4 nodes, 3 grounded edges, and 3 passage evidence items"
  artifacts:
    - path: "app/tool/page.tsx"
      provides: "Compact synthesis result status indicator"
      min_lines: 1200
---

<objective>
Wire provenance outcomes through the streaming pipeline, Supabase telemetry, typed client parsing, and `/tool` so users and developers can distinguish grounded, degraded, and fallback graph synthesis runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-graph-synthesis-quality-provenance/05-CONTEXT.md
@.planning/phases/05-graph-synthesis-quality-provenance/05-RESEARCH.md
@.planning/phases/05-graph-synthesis-quality-provenance/05-PATTERNS.md
@lib/pipeline/stream-handler.ts
@lib/pipeline/types.ts
@hooks/use-semantic-universe-stream.ts
@app/tool/page.tsx
@supabase/migrations/20260506200000_observability_tables.sql
@supabase/migrations/20260506210000_phase4_retrieval_stats.sql
</context>

<tasks>

<task type="manual">
  <name>Task 1: Add additive telemetry migration for degraded synthesis</name>
  <files>supabase/migrations/20260508060000_phase5_synthesis_provenance.sql</files>
  <read_first>
    - supabase/migrations/20260506200000_observability_tables.sql
    - supabase/migrations/20260506210000_phase4_retrieval_stats.sql
    - .planning/phases/05-graph-synthesis-quality-provenance/05-RESEARCH.md Telemetry guidance
  </read_first>
  <action>
    Create an additive Supabase migration named `20260508060000_phase5_synthesis_provenance.sql`.

    Required SQL:
    - Replace or relax the existing `semantic_pipeline_runs.result_type` check so `result_type` accepts `success`, `degraded`, `fallback`, and `error`.
    - Add nullable column `synthesis_result_reason text`.
    - Add nullable column `synthesis_repaired_links integer`.
    - Add nullable column `synthesis_rejected_links integer`.
    - Add nullable column `synthesis_rejected_nodes integer`.
    - Add nullable column `synthesis_grounded_edge_count integer`.
    - Add nullable column `synthesis_passage_evidence_count integer`.
    - Do not relax RLS policies.

    [BLOCKING] Schema Push Required:
    - ORM detected: Supabase
    - Push command: `supabase db push`
    - Non-TTY workaround: set `SUPABASE_ACCESS_TOKEN` if login is unavailable
    - Run `supabase db push` after this migration exists and before final verification for Phase 5.
  </action>
  <verify>
    <automated>rg 'degraded|synthesis_result_reason|synthesis_grounded_edge_count' supabase/migrations/20260508060000_phase5_synthesis_provenance.sql</automated>
    <manual>`supabase db push` completes against the intended Supabase project</manual>
  </verify>
  <acceptance_criteria>
    - Migration contains literal `degraded`
    - Migration contains `synthesis_result_reason`
    - Migration does not contain `drop table`
    - Phase verification records the `supabase db push` result
  </acceptance_criteria>
  <done>Telemetry can persist degraded synthesis without violating the existing result_type check constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Carry result type and provenance counts through stream handler</name>
  <files>lib/pipeline/stream-handler.ts, lib/pipeline/__tests__/stream-handler.test.ts</files>
  <read_first>
    - lib/pipeline/stream-handler.ts
    - lib/pipeline/structurer.ts return shape from Plan 05-02
    - lib/pipeline/provenance.ts
    - lib/pipeline/__tests__/stream-handler.test.ts
  </read_first>
  <action>
    Update `runSemanticUniverseAnalysisStream` to consume the provenance result returned by `synthesizeWithOpenAI`.

    Required behavior:
    - `result_type` local type becomes `"success" | "degraded" | "fallback"`.
    - Set `result_type = syn.provenance.resultType` when synthesis succeeds and validation returns success/degraded.
    - Set `result_type = "fallback"` and `resultReason = "fallback_synthesis"` or `below_grounded_graph_floor` when catch/buildFallback path is used.
    - Add `payload.resultType` and `payload.resultReason` before image enrichment and final `done`.
    - Include concise synthesis done detail: `result: success`, `result: degraded (...)`, or `result: fallback (...)`.
    - Persist `synthesis_result_reason`, `synthesis_repaired_links`, `synthesis_rejected_links`, `synthesis_rejected_nodes`, `synthesis_grounded_edge_count`, and `synthesis_passage_evidence_count`.
    - Do not fail the user run if telemetry insert fails.

    Update stream-handler tests for success, degraded, and fallback paths.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/stream-handler.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'synthesis_result_reason' lib/pipeline/stream-handler.ts`
    - `grep -q 'resultReason' lib/pipeline/stream-handler.ts`
    - Stream-handler tests assert final done payload has `resultType`
    - Stream-handler tests assert telemetry receives `result_type: "degraded"` for degraded provenance
  </acceptance_criteria>
  <done>The server stream and telemetry agree on whether synthesis was grounded, degraded, or fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Extend typed stream contract and client payload handling</name>
  <files>lib/pipeline/types.ts, hooks/use-semantic-universe-stream.ts, lib/pipeline/__tests__/pipeline.contract.test.ts</files>
  <read_first>
    - lib/pipeline/types.ts
    - hooks/use-semantic-universe-stream.ts
    - lib/pipeline/__tests__/pipeline.contract.test.ts
    - .planning/phases/01-stream-contract-foundation-types/01-CONTEXT.md fail-fast stream rules
  </read_first>
  <action>
    Preserve fail-fast NDJSON parsing while adding contract coverage for final payload result metadata.

    Required behavior:
    - Export or locally define `resultType` literals `success`, `degraded`, and `fallback` in the contract layer if needed.
    - Add a contract test where a `done` payload contains `resultType: "degraded"` and `resultReason: "unsupported_relationships_removed"`.
    - Add a negative contract or stream-handler test for invalid result type if the done payload schema becomes typed.
    - `useSemanticUniverseStream` does not need a new event kind; it should continue dispatching `done` with payload metadata.
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/pipeline.contract.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'degraded' lib/pipeline/__tests__/pipeline.contract.test.ts`
    - `grep -q 'resultReason' lib/pipeline/__tests__/pipeline.contract.test.ts`
    - No new stream `type` discriminator is added for result status
  </acceptance_criteria>
  <done>Typed stream parsing accepts the result metadata needed by `/tool` without weakening Phase 1 fail-fast behavior.</done>
</task>

<task type="auto">
  <name>Task 4: Add compact `/tool` synthesis status indicator</name>
  <files>app/tool/page.tsx</files>
  <read_first>
    - app/tool/page.tsx local payload types and trace UI
    - hooks/use-semantic-universe-stream.ts done callback usage
    - .planning/phases/05-graph-synthesis-quality-provenance/05-CONTEXT.md D-07 through D-09
  </read_first>
  <action>
    Add a compact status indicator to the existing `/tool` analysis surface.

    Required behavior:
    - Local `SemanticUniversePayload` type includes optional `resultType?: "success" | "degraded" | "fallback"` and `resultReason?: string`.
    - Component state stores the latest result type and reason from the `done` payload.
    - Indicator copy uses:
      - `Grounded synthesis` for success
      - `Partial graph: unsupported relationships removed` for degraded
      - `Fallback graph: not enough grounded evidence` for fallback
    - Indicator appears near the existing run/trace/progress area or graph header and remains compact.
    - Do not build the Phase 7 evidence panel.
    - Do not hide the existing evidence explorer.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Grounded synthesis' app/tool/page.tsx`
    - `grep -q 'Partial graph: unsupported relationships removed' app/tool/page.tsx`
    - `grep -q 'Fallback graph: not enough grounded evidence' app/tool/page.tsx`
    - `grep -q 'resultReason' app/tool/page.tsx`
  </acceptance_criteria>
  <done>Users can immediately distinguish primary, degraded, and fallback graph runs in `/tool`.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-05-07 | Repudiation | Result type, reason, and provenance counts are persisted in `semantic_pipeline_runs`. |
| T-05-08 | Information Disclosure | UI status displays concise degradation reasons, not full rejected evidence bodies or raw provider payloads. |
| T-05-09 | Tampering | Stream result metadata is produced server-side after validator decisions, not accepted from client input. |
| T-05-10 | Elevation of Privilege | Supabase migration must not alter RLS policies for `semantic_pipeline_runs`. |
</threat_model>

<verification>
- `npx vitest run lib/pipeline/__tests__/pipeline.contract.test.ts lib/pipeline/__tests__/stream-handler.test.ts`
- `npm test`
- `npx tsc --noEmit`
- Manual/blocking before final Phase 5 verification: `supabase db push`
- Manual `/tool` smoke: run a known brand and confirm the compact result status appears after completion
</verification>

<success_criteria>
SYN-02 is satisfied when the final stream payload, telemetry row, and `/tool` UI all expose `success`, `degraded`, or `fallback` with a concise reason, and SYN-01 remains visible through grounded edge counts and non-empty link evidence/source IDs.
</success_criteria>

