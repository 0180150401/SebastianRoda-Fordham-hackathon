---
phase: 02-observability-security-cost-controls
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - package-lock.json
  - lib/pipeline/types.ts
  - lib/pipeline/__tests__/pipeline.contract.test.ts
  - lib/langfuse/client.ts
  - lib/openai/traced-client.ts
  - app/api/semantic-universe/route.ts
  - hooks/use-semantic-universe-stream.ts
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "`run_meta` NDJSON event carries UUID `run_id` correlating stream ↔ Langfuse ↔ `semantic_pipeline_runs.run_id`"
    - "Primary OpenAI synthesis uses instrumented OpenAI SDK (`observeOpenAI`) when Langfuse keys enabled"
    - "Completed semantic-universe runs persist telemetry row including stage durations, token counts, graph counts, `result_type`"
  artifacts:
    - path: "lib/langfuse/client.ts"
      provides: "singleton Langfuse + helper `isLangfuseEnabled()`"
      min_lines: 15
    - path: "lib/openai/traced-client.ts"
      provides: "`createTracedOpenAI(apiKey: string)` returning OpenAI instance"
      min_lines: 20
  key_links:
    - from: "semantic-universe route"
      to: "semantic_pipeline_runs"
      via: "supabase insert after stream completes"
      pattern: "semantic_pipeline_runs"
---

<objective>
Deliver OBS-01 dual telemetry on the **semantic-universe** path: Langfuse-instrumented OpenAI calls plus durable Supabase **`semantic_pipeline_runs`** rows. Extend the Phase 1 **`PipelineEvent`** contract with a leading **`run_meta`** event so `/tool` consumers remain backward-compatible while gaining correlation IDs. Install **`langfuse@^3`** and **`openai@^6`** (research-approved) and replace the raw `fetch("https://api.openai.com/v1/chat/completions")` usage inside `synthesizeWithOpenAI` with the traced SDK client.

**Explicitly out of this plan:** geo-chat route changes (Plan 03), kill switch + daily cap enforcement ordering (Plan 03), migration authoring (Plan 01).

Gate: Plan 01 migration must exist locally before inserts land — executor should handle graceful failure logging if tables missing in dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-observability-security-cost-controls/02-CONTEXT.md
@.planning/phases/02-observability-security-cost-controls/02-RESEARCH.md
@lib/pipeline/types.ts
@app/api/semantic-universe/route.ts
@hooks/use-semantic-universe-stream.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add langfuse + openai dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - `.planning/STATE.md` approved ranges (`langfuse@^3`, `openai@^6.35`)
  </read_first>
  <action>
    Run:
    ```bash
    npm install langfuse@^3 openai@^6
    ```

    Do not remove existing dependencies.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies?.langfuse||!p.dependencies?.openai) throw new Error('missing deps'); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"langfuse"' package.json` matches
    - `grep '"openai"' package.json` matches
    - `npm ls langfuse openai` exits 0
  </acceptance_criteria>
  <done>Tracing-capable SDKs installed.</done>
</task>

<task type="auto">
  <name>Task 2: Langfuse + traced OpenAI factory helpers</name>
  <files>lib/langfuse/client.ts, lib/openai/traced-client.ts</files>
  <read_first>
    - `.planning/research/STACK.md` § Langfuse keys + CI guard
    - `.env.example` LANGFUSE_* entries added in Plan 01 Task 3
  </read_first>
  <action>
    Create **`lib/langfuse/client.ts`** exporting:
    - `isLangfuseEnabled(): boolean` — true only when `process.env.LANGFUSE_ENABLED !== 'false'` AND both `LANGFUSE_SECRET_KEY` and `LANGFUSE_PUBLIC_KEY` are non-empty after trimming.
    - `getLangfuse()` — returns configured `Langfuse` SDK instance or `null` when disabled.

    Create **`lib/openai/traced-client.ts`** exporting:
    ```ts
    import OpenAI from "openai";
    export function createTracedOpenAI(apiKey: string): OpenAI
    ```
    Implementation rules:
    - If `isLangfuseEnabled()` → import `observeOpenAI` from `langfuse` (named export per Langfuse v3 docs) and wrap: `observeOpenAI(new OpenAI({ apiKey }), { langfuseClient: getLangfuse()! })` — adjust to official signature if differs (read node_modules types).
    - Else return plain `new OpenAI({ apiKey })`.

    Never throw when Langfuse disabled — degrade silently.

    Add bracketed log prefix `[langfuse]` only for initialization warnings (optional).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/langfuse/client.ts`
    - `test -f lib/openai/traced-client.ts`
    - `grep -q "isLangfuseEnabled" lib/langfuse/client.ts`
    - `grep -q "createTracedOpenAI" lib/openai/traced-client.ts`
    - `grep -q "observeOpenAI" lib/openai/traced-client.ts`
  </acceptance_criteria>
  <done>Routes can construct one traced client per request without duplicating env parsing.</done>
</task>

<task type="auto">
  <name>Task 3: Extend `PipelineEvent` with `run_meta` + contract tests</name>
  <files>lib/pipeline/types.ts, lib/pipeline/__tests__/pipeline.contract.test.ts, hooks/use-semantic-universe-stream.ts</files>
  <read_first>
    - lib/pipeline/types.ts (existing discriminated union)
    - `.planning/phases/01-stream-contract-foundation-types/01-CONTEXT.md` D-01 fail-fast semantics
  </read_first>
  <action>
    1. Add zod schema:
       ```ts
       const runMetaEventSchema = z.object({
         type: z.literal("run_meta"),
         run_id: z.string().uuid(),
       });
       ```
       Append to `z.discriminatedUnion("type", [runMetaEventSchema, stepEventSchema, ...])` — **order**: place `run_meta` first in union array (zod discriminated union order does not fix emission order; tests must accept).

    2. Export inferred type `RunMetaEvent`.

    3. Update **`hooks/use-semantic-universe-stream.ts`** `dispatch` loop to explicitly ignore `run_meta` (no callback required) — add comment "correlation only".

    4. Extend **`lib/pipeline/__tests__/pipeline.contract.test.ts`** with at least one fixture NDJSON chunk containing `{"type":"run_meta","run_id":"<uuid>"}` followed by existing step events — assert `pipelineEventSchema.safeParse` succeeds for run_meta line.

    Do **not** change server emission yet (next task).
  </action>
  <verify>
    <automated>npx vitest run lib/pipeline/__tests__/pipeline.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'literal("run_meta")' lib/pipeline/types.ts`
    - Contract test file mentions `run_meta` literal substring at least once
    - `npm test` exits 0 (or targeted vitest command passes)
  </acceptance_criteria>
  <done>Wire format extended without breaking zod validation.</done>
</task>

<task type="auto">
  <name>Task 4: Refactor `synthesizeWithOpenAI` to SDK + return token usage</name>
  <files>app/api/semantic-universe/route.ts</files>
  <read_first>
    - app/api/semantic-universe/route.ts (function `synthesizeWithOpenAI` lines ~1478-1576)
    - lib/openai/traced-client.ts
  </read_first>
  <action>
    Change signature to accept `openai: OpenAI` instance instead of raw key usage inside — caller passes `createTracedOpenAI(openAiKey)` once per stream.

    Replace `fetch(...)` with `openai.chat.completions.create({ model: "gpt-4.1-mini", temperature: 0.3, messages: [...], response_format: { type: "json_object" } })`.

    Return shape **`{ payload: SemanticUniversePayload; usage: { inputTokens: number; outputTokens: number } }`** where usage reads `completion.usage?.prompt_tokens ?? 0` and `completion.usage?.completion_tokens ?? 0`.

    Preserve existing prompt text, JSON extraction (`extractJsonObject`), and `normalizeModelPayload` behavior.

    Update **all call sites** inside the same file (stream handler + any tests if referenced).

    On SDK errors, propagate same throw semantics as today (caller catch triggers fallback).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "chat.completions.create" app/api/semantic-universe/route.ts`
    - Shell test `! rg -q "api\\.openai\\.com/v1/chat/completions" app/api/semantic-universe/route.ts` passes **after** refactor (no raw REST call remains for synthesis)
    - `grep -q "inputTokens" app/api/semantic-universe/route.ts`
  </acceptance_criteria>
  <done>Synthesis path emits measurable token counts.</done>
</task>

<task type="auto">
  <name>Task 5: Instrument semantic-universe stream — run_meta, timings, DB insert</name>
  <files>app/api/semantic-universe/route.ts</files>
  <read_first>
    - POST handler + stream start section (~1584-1689)
    - `@/lib/supabase/server` createClient pattern (reuse authenticated client)
    - `.planning/phases/02-observability-security-cost-controls/02-CONTEXT.md` D-02 correlation rules
  </read_first>
  <action>
    Inside `POST` **before** constructing `ReadableStream`, instantiate `const runId = crypto.randomUUID()` (server runtime provides global `crypto`).

    Pass `runId`, `user.id`, `brand`, `supabase` handle, and traced OpenAI client into stream closure.

    At **`start(controller)` first line** (before any external IO except cookies already resolved): `emitLine(controller, { type: "run_meta", run_id: runId });`

    Measure durations using `performance.now()` or `Date.now()` deltas:
    - `duration_ms_sources` — Tavily+Exa parallel fetch segment
    - `duration_ms_synthesis` — `synthesizeWithOpenAI` await (including fallback catch — still measure wall time)
    - `duration_ms_images` — `enrichVisualCorrelationsWithImages`

    Capture **`result_type`**: `'success'` when synthesis try succeeds without entering fallback catch; `'fallback'` when catch uses `buildFallback`; `'error'` when outer catch emits error event before completion.

    Token accounting for DB row: sum synthesis `usage.inputTokens` + `usage.outputTokens`. Future geo-chat increments handled separately (Plan 03).

    After payload finalized (just before emitting `{type:'done'}`), **`await supabase.from('semantic_pipeline_runs').insert({...})`** — fields must match migration columns:
    `run_id`, `user_id`, `brand`, `completed_at: new Date().toISOString()`, durations, token columns, `retrieval_units: 1` (one paired retrieval cycle), counts (`nodes.length`, `links.length`, `sources.length`), `result_type`, optional `error_message`, `langfuse_trace_id` when obtainable from Langfuse helper metadata (nullable acceptable if unavailable).

    Log insert failures with `[semantic-universe]` prefix — **do not** fail user stream solely on telemetry insert failure (document trade-off inline comment).

    Export **`export const maxDuration = 300`** from this route module (Vercel / Next route segment config) per CONTEXT D-08 wall-clock mitigation.

    Ensure TypeScript compile passes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'type: "run_meta"' app/api/semantic-universe/route.ts`
    - `grep -q "semantic_pipeline_runs" app/api/semantic-universe/route.ts`
    - `grep -q "maxDuration" app/api/semantic-universe/route.ts`
    - `grep -q "createTracedOpenAI" app/api/semantic-universe/route.ts`
  </acceptance_criteria>
  <done>Happy-path runs produce Langfuse trace (when enabled) plus queryable DB row keyed by `run_id`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Langfuse Cloud ↔ OpenAI payloads | Prompt text includes brand + evidence snippets |
| Supabase telemetry INSERT | Uses authenticated user session |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Langfuse stores prompts | mitigate | Disable via `LANGFUSE_ENABLED=false` in dev; document in `.env.example` |
| T-02-05 | Tampering | Client emits fake `run_meta` | mitigate | Server generates authoritative meta line — clients cannot spoof server stream ordering |
| T-02-06 | Denial of Service | Extra DB write per run | mitigate | Async insert errors swallowed with logs — stream unaffected |
</threat_model>

<verification>
- `npx tsc --noEmit`
- `npm test`
- Manual smoke: run `/tool` semantic analysis — NDJSON first line parses as `run_meta` (browser devtools → stream); Langfuse dashboard optional when keys configured
</verification>

<success_criteria>
- Roadmap SC1 satisfied for semantic-universe primary path (latency buckets + tokens + counts + `result_type` available from SQL + Langfuse when configured).
</success_criteria>
