---
phase: 02-observability-security-cost-controls
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - lib/observability/kill-switch.ts
  - lib/usage/daily-token-budget.ts
  - app/api/semantic-universe/route.ts
  - app/api/geo-chat/route.ts
  - lib/pipeline/__tests__/pipeline.contract.test.ts
autonomous: true
requirements: [OBS-02, OBS-03]
must_haves:
  truths:
    - "`SEMANTIC_PIPELINE_DISABLED` forces **503** `{ code: \"PIPELINE_DISABLED\" }` on new semantic-universe and geo-chat requests"
    - "Authenticated users exceeding UTC-day pseudo-token budget receive **429** `{ code: \"DAILY_CAP\" }` before paid upstream calls"
    - "`/api/geo-chat` returns **401** without Supabase session and shares kill switch + cap + traced OpenAI stack"
  artifacts:
    - path: "lib/usage/daily-token-budget.ts"
      provides: "read rollup + guard + apply consumption helpers"
      min_lines: 40
  key_links:
    - from: "semantic-universe POST"
      to: "user_daily_usage"
      via: "pre-check + post-run increment"
      pattern: "user_daily_usage"
---

<objective>
Close OBS-02 (kill switch + daily pseudo-token budget without silent overruns) and OBS-03 (geo-chat authentication + paid-work parity). Compose helpers consumed by both routes so caps stay consistent. Upgrade **`/api/geo-chat`** to **`createClient()` + `getUser()` guard**, reuse **`createTracedOpenAI`**, record token usage into **`user_daily_usage`**, and keep JSON response shape `{ reply }` unchanged for clients.

Depends on Plans **02-01** (tables) and **02-02** (`run_meta`, traced synthesis instrumentation patterns).

Explicit trade-off accepted: concurrent requests may race rollup totals — mitigation documented inline; Phase 2 prioritizes deterministic env knobs over distributed locks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-observability-security-cost-controls/02-CONTEXT.md
@app/api/geo-chat/route.ts
@app/api/semantic-universe/route.ts
@lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill switch helper</name>
  <files>lib/observability/kill-switch.ts</files>
  <read_first>
    - `.planning/phases/02-observability-security-cost-controls/02-CONTEXT.md` § D-07
    - `.env.example` `SEMANTIC_PIPELINE_DISABLED`
  </read_first>
  <action>
    Create `lib/observability/kill-switch.ts` exporting:

    ```ts
    export function isSemanticPipelineDisabled(): boolean {
      const v = process.env.SEMANTIC_PIPELINE_DISABLED?.trim().toLowerCase();
      return v === "1" || v === "true" || v === "yes";
    }
    ```

    No side effects on import.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "isSemanticPipelineDisabled" lib/observability/kill-switch.ts`
    - File length ≥ 8 lines
  </acceptance_criteria>
  <done>Kill switch semantics centralized.</done>
</task>

<task type="auto">
  <name>Task 2: Daily pseudo-token budget helpers</name>
  <files>lib/usage/daily-token-budget.ts</files>
  <read_first>
    - `.planning/phases/02-observability-security-cost-controls/02-CONTEXT.md` § D-03–D-06
    - Supabase typed client patterns from semantic-universe profile queries
  </read_first>
  <action>
    Implement **`lib/usage/daily-token-budget.ts`** with pure parsers:

    ```ts
    export function getSemanticDailyTokenBudget(): number; // default 500_000 when unset/invalid
    export function getRetrievalUnitTokenCharge(): number; // default 500 when unset/invalid
    ```

    Implement **`getUtcDateString(date = new Date())`** returning `YYYY-MM-DD` in UTC.

    Implement **`async loadDailyRollup(supabase, userId: string)`** → `{ input: number; output: number; retrievalUnits: number }` reading `user_daily_usage` for `(user_id, usage_date)` PK — zeros when missing.

    Implement **`pseudoTokensUsed(rollup)`** returning `rollup.input + rollup.output + rollup.retrievalUnits * getRetrievalUnitTokenCharge()`.

    Implement **`async assertDailyBudgetAllows(supabase, userId: string): Promise<void>`** — throws `DailyCapError` (custom Error subclass with `code = 'DAILY_CAP'`) when `pseudoTokensUsed(loadDailyRollup(...)) >= getSemanticDailyTokenBudget()`.

    Implement **`async applyOpenAiUsage(supabase, userId: string, usage: { input: number; output: number }, retrievalUnitsDelta: number)`** — upsert/increment row for today's UTC date:
    - If row missing: insert totals.
    - Else: add to existing counters using `.update` after `.select` OR Supabase `.upsert` with additive semantics — choose one documented approach.

    Export typed error for routes to map to HTTP 429 JSON `{ error: string, code: 'DAILY_CAP' }`.
  </action>
  <verify>
    <automated>npx vitest run lib/usage/daily-token-budget.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/usage/daily-token-budget.ts`
    - `test -f lib/usage/daily-token-budget.test.ts`
    - Tests cover: parser defaults, `pseudoTokensUsed` math with retrieval charge, `assertDailyBudgetAllows` throws when at budget boundary
    - `grep -q "DAILY_CAP" lib/usage/daily-token-budget.ts`
  </acceptance_criteria>
  <done>Budget logic unit-tested independent of HTTP layer.</done>
</task>

<task type="auto">
  <name>Task 3: Wire kill switch + cap ordering into semantic-universe POST</name>
  <files>app/api/semantic-universe/route.ts</files>
  <read_first>
    - Current POST handler ordering (~1584 onward)
    - lib/observability/kill-switch.ts
    - lib/usage/daily-token-budget.ts
  </read_first>
  <action>
    At top of `POST`, immediately after constructing helpers imports:

    1. If `isSemanticPipelineDisabled()` → `return NextResponse.json({ error: "Semantic pipeline temporarily disabled.", code: "PIPELINE_DISABLED" }, { status: 503 });`

    2. Keep existing auth (`401`) and paywall (`402 PAYWALL`) ordering unchanged relative to each other.

    3. After authenticated user resolved AND paywall passes, **`await assertDailyBudgetAllows(supabase, user.id)`** inside try/catch — map `DailyCapError` to `429` JSON `{ error: "Daily analysis budget exceeded. Try again tomorrow.", code: "DAILY_CAP" }` per CONTEXT D-06.

    4. After successful synthesis metrics known (Plan 02 returned usage) **and** before responding completion, call **`applyOpenAiUsage`** adding synthesis tokens **plus** `retrievalUnitsDelta = 1` for semantic-universe runs that executed Tavily+Exa pair.

    5. On telemetry paths that already insert `semantic_pipeline_runs`, keep behavior — budget rollup must still reflect consumption even if insert fails.

    Do not charge retrieval unit tokens when kill-switch abort prevents retrieval (obviously unreachable).

    Ensure imports never create circular dependency with tracing helpers.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "isSemanticPipelineDisabled" app/api/semantic-universe/route.ts`
    - `grep -q "assertDailyBudgetAllows" app/api/semantic-universe/route.ts`
    - `grep -q "applyOpenAiUsage" app/api/semantic-universe/route.ts`
    - `grep -q "PIPELINE_DISABLED" app/api/semantic-universe/route.ts`
    - `grep -q "DAILY_CAP" app/api/semantic-universe/route.ts`
  </acceptance_criteria>
  <done>Semantic route honors OPS toggles and prevents capped users from starting new paid runs.</done>
</task>

<task type="auto">
  <name>Task 4: Secure and instrument geo-chat</name>
  <files>app/api/geo-chat/route.ts</files>
  <read_first>
    - app/api/geo-chat/route.ts full file
    - app/api/semantic-universe/route.ts auth header pattern
    - lib/openai/traced-client.ts
    - lib/usage/daily-token-budget.ts
    - lib/observability/kill-switch.ts
  </read_first>
  <action>
    1. Import `createClient` from `@/lib/supabase/server`.

    2. First lines inside `POST` after try opens: kill-switch identical JSON as semantic route (`503` / `PIPELINE_DISABLED`).

    3. `const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser();` — if `!user` return `401` `{ error: "Unauthorized" }` matching semantic-universe wording.

    4. Call `assertDailyBudgetAllows(supabase, user.id)` — catch `DailyCapError` → `429` / `DAILY_CAP`.

    5. Replace `fetch("https://api.openai.com/v1/chat/completions", ...)` with `createTracedOpenAI(openAiKey).chat.completions.create(...)` mirroring model `gpt-4.1-mini`, temperature `0.35`, messages array unchanged.

    6. After success, read usage tokens like Plan 02 and invoke `applyOpenAiUsage(supabase, user.id, usage, 0)` (**zero** retrieval units).

    7. Preserve existing `400` / `500` behaviors for validation/OpenAI failure — but ensure OpenAI failures **do not** increment budget.

    Optional (nice): attach Langfuse metadata `{ route: "geo-chat", brand }` via SDK helper if supported without scope creep — skip if not trivial.

    Ensure **no** `semantic_demo_used` cookie logic — unrelated.

    Remove unused fetch-specific typings if redundant.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getUser()" app/api/geo-chat/route.ts`
    - `grep -q "createTracedOpenAI" app/api/geo-chat/route.ts`
    - `grep -q "PIPELINE_DISABLED" app/api/geo-chat/route.ts`
    - `grep -q "DAILY_CAP" app/api/geo-chat/route.ts`
    - `grep -q 'api.openai.com/v1/chat/completions' app/api/geo-chat/route.ts` exits status **1** (REST removed)
  </acceptance_criteria>
  <done>Anonymous browsers cannot spend OpenAI credits via geo-chat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous HTTP → geo-chat | Previously allowed paid completion — now blocked at `401` |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Spoofing | Session-less geo-chat abuse | mitigate | `getUser()` gate mirrors semantic-universe |
| T-02-07 | Denial of Service | malicious account consumes shared budget | mitigate | UTC rollup + env-wide budget |
| T-02-08 | Elevation | Kill switch bypass via alternate endpoint | mitigate | Flag honored identically on both routes in-scope |
</threat_model>

<verification>
- `npm test`
- `npx tsc --noEmit`
- Manual curl without cookies → `/api/geo-chat` returns `401`
- Manual env `SEMANTIC_PIPELINE_DISABLED=true` → both routes `503`
</verification>

<success_criteria>
- Roadmap SC2–SC4 behaviors evidenced via automated tests + documented manual curls.
- REQUIREMENTS.md checkboxes for OBS-02 / OBS-03 ready for human verification post-execution.
</success_criteria>
